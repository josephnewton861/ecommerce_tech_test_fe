import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ecommerce_tech_test_fe',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
